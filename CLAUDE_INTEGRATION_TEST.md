# Claude Integration Test

This file was created by Claude Code as an integration test of its GitHub workflow:
creating a branch, committing a change, pushing, and opening a pull request with the `gh` CLI.

It has no effect on the test harness and can be safely deleted once the PR has been reviewed.
